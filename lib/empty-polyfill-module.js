// fallow-ignore-file unused-file
// Empty stub aliased over Next.js's client polyfill module in next.config.mjs.
// The real module ships ~14 KiB of conditional polyfills for browsers below
// the production browserslist target - every supported browser already has
// those methods natively, so the bytes are dead code on every page load.
export {};
